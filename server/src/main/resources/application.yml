spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/habitdb?serverTimezone=UTC
    username: habituser
    password: secret
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration